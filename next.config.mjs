/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['openweathermap.org','82.137.255.6','127.0.0.1'], // السماح بتحميل الصور من openweathermap.org
      },
};

export default nextConfig;
